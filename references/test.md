# /test routing

Goal: validate the requested behavior at the most relevant level.

Start with `test-driven-development`. Add `browser-testing-with-devtools` for real browser behavior, `performance-optimization` for measurable performance criteria, and `source-driven-development` if compatibility hinges on a platform contract. Prefer targeted evidence over broad, unfocused coverage work.
