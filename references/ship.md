# /ship routing

Goal: prepare the requested delivery or release safely.

Start with `shipping-and-launch`. Add `git-workflow-and-versioning` for release history or versioning, `ci-cd-and-automation` for pipeline work, `observability-and-instrumentation` for production readiness, `deprecation-and-migration` for rollout compatibility, and `documentation-and-adrs` for release-facing decisions. Select only what the actual release scope needs.
